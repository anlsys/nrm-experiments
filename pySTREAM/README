* Choose the number of threads by exporting OMP_NUM_THREADS environment variable
* Change the "bench" variable in launch/launcher.sh to select the wanted benchmark
* Use ./launcher.sh in a Nix shell with NRM
